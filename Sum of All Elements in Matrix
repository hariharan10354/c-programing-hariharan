#include <stdio.h>

int main() {
    int a[3][3];
    int i, j;
    int sum = 0;

    // Read matrix elements
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 3; j++) {
            scanf("%d", &a[i][j]);
            sum += a[i][j];
        }
    }

    // Print sum
    printf("%d", sum);

    return 0;
}
