#include <stdio.h>

void swap(int *x, int *y) {
    int temp = *x;
    *x = *y;
    *y = temp;
}

int main() {
    int A, B;

    scanf("%d %d", &A, &B);

    swap(&A, &B);

    printf("%d %d", A, B);

    return 0;
}
